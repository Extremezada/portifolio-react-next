import style from '../../styles/Header.module.css'

export default function Header(){
    return(
        <header className={style.flex}>
            <h1>Criar layout Cabeçalho</h1>
        </header>
    )
}