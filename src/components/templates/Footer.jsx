import style from "../../styles/Footer.module.css"

export default function Footer(props) {
    return (
        <footer className={style.flex}>
            <h1>Colocar Layout Rodapé</h1>
        </footer>
    )
}